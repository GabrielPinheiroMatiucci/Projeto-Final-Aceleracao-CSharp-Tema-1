// Network
docker network create networkTryitter

docker network rm networkTryitter


// Banco de dados
docker run -d --rm --name mysqlTryitter -e MYSQL_ROOT_PASSWORD=root -e MYSQL_ALLOW_EMPTY_PASSWORD=false --mount src=volumeTryitterDatabase,target=/var/lib/mysql --network networkTryitter -p 3333:3306 mysql

docker container stop mysqlTryitter


// API
docker build ./Tryitter -t apitryitter && docker run -d --rm --name apiTryitter -p 5000:80 --network networkTryitter apitryitter

docker container stop apiTryitter
